"""import subprocess

def run_file(file_path):
    subprocess.call(file_path, shell=True)

# Example usage
file_path = r'E:\A.D.A\A.D.A\loadup\wake\art.bat'  # Path to the BAT file you want to run
run_file(file_path)

# Continue with the next line of code
print("Continuing to the next line...")
            while True:
                if time.time()-start>=timeout:
                    self.open_file(f"{self.drive}/A.D.A/A.D.A/loadup/wake/terminate.bat")
                    exit()
                else:
                    if open<1:
                        self.open_file(self.cmds)
                        print("its not working")
                        open+=1"""